# Conexão com o PostgreSQL local (Docker Compose)
DATABASE_URL="postgresql://usuario:senha@localhost:5432/qualityhub?schema=public"

# Chave de assinatura dos tokens de Login (gerado usando "openssl rand -base64 32")
JWT_SECRET="valor-gerado-aqui"